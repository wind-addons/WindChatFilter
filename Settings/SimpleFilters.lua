local W, F, L, P = unpack(select(2, ...))

P.simpleFilters = {
    enable = true,
    useCahce = true,
    noPadarenDK = true,
    smartNameFilter = true,
    smartMassegeFilter = true,
    addonMessageFilter = true,
    factionFilter = false,
}
